server:
  port: 9093
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: delivery
  activemq:
    packages:
      trust-all:
integration:
  client:
    url: http://localhost:${wiremock.server.port}